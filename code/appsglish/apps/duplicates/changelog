
<change id=0087>
 <author> Wim Brouw </author>
 <date> 2001/03/21 </date>
 <avers> 1.5 </avers>
 <area> System </area>
 <package> appsglish </package>
 <module> duplicates </module>
 <type> Code </type>
 <category> Change </category>
 <defects>  </defects>
 <summary>
Added an -s switch and more typedef checks
 </summary>
 <description>
I added an -s (for system) switch to limit the output of the duplicates program to duplicates that should be removed.

In addition I added more typedef definitions (like e.g. MS, LogicalArray) to be able to detect duplicates whether typedefs are used or not.

In general the use of typedefs should be carefully considered in templates files: there could be an unintended clash of names if typedefs are just used for local convenience.
 </description>
</change>
